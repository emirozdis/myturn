# Web Push (generate your own with: npx web-push generate-vapid-keys)
# Use https://localhost:3000 or http://localhost:3000 — not a LAN IP (192.168.x.x).
NEXT_PUBLIC_VAPID_KEY=***
VAPID_PRIVATE_KEY=***
VAPID_SUBJECT=mailto:you@example.com
